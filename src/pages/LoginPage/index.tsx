import { Header } from "@features/Header";
import { LoginScreen } from "@widgets/LoginPage";

export const LoginPage = () => {
  return (
    <div className="page">
      <Header />
      <LoginScreen />
    </div>
  );
};
