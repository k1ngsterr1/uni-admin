import { Header } from "@features/Header";
import { StartScreen } from "@widgets/StartPage";

export const StartPage = () => {
  return (
    <div className="page">
      <Header />
      <StartScreen />
    </div>
  );
};
